<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login</title>
    <link rel="stylesheet" href="/stylesheets/create-team.css" />
  </head>
  <body>
    <%- include("./partials/navbar", {currentUser: currentUser}) %>
    <div class="main-container">
      <div class="fields-and-links">
        <h1>Create your team</h1>
        <form action="/createTeam" method="post">
          <div class="fields">
            <!-- <div class="label-input">
              <label for="teamName">Team Name</label>
              <input type="text" name="teamName" id="teamName" />
            </div> -->
            <div class="label-input">
              <label for="idea">Project idea</label>
              <input type="idea" name="idea" id="idea" />
            </div>

            <div class="label-input">
              <label for="description">Current description:</label>
              <textarea
                class="ideas"
                name="description"
                id="description"
                cols="30"
                rows="10"
              ></textarea>
            </div>
          </div>
            
            <button class="create-team-button" type="submit">Create Team</button>
            <% if(locals.error){ %>
              <h3><%= error %></h3>
              <% } %>
        </form>
      </div>

      <div class="main-img">
        <img
          src="/images/team-working.png"
          alt="team working"
          class="main-img"
        />
      </div>
    </div>
    <script src="/javascripts/navbar.js"></script>
  </body>
</html>
